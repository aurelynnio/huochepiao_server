spring:
  application:
    name: search-service

server:
  port: ${SERVER_PORT:8085}

app:
  internal:
    api-key: ${APP_INTERNAL_API_KEY:vetau-internal-key}

search:
  elasticsearch:
    url: ${ELASTICSEARCH_URL:http://localhost:9200}
    ticket-index: ${SEARCH_TICKET_INDEX:ticket-search}
